# Legacy Template Compatibility

The current backend includes deterministic families such as mounting plates,
enclosures, brackets, spacers, caps, trays, clips, and covers.

They are useful regression fixtures and fast-path implementations. They do not
define CadFlow's target design space and should not be extended by default when
a generalized feature graph or sandboxed model program can express the need.
