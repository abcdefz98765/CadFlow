# Part Request notes

The request identifies `upper_link` as the selected assembly candidate and
narrows execution to one part. It carries interface and assembly context, but
contains no provider-generated CAD IR or executable code.
