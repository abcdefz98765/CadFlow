# Planning notes

Planning decomposes the assembly into six printable candidates and two
reference components. Only `upper_link` enters the reviewed-part pipeline. The
plan is not authorization to generate every part or a combined assembly.
