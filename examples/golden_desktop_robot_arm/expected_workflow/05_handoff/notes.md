# Reviewed Handoff notes

This is the authoritative input to `AgentAdapter.create_part_ir(...)`. The
action layer passes the reviewed definition onward; it does not select a CAD
template or hard-code geometry.
